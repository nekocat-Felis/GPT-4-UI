# 設定などを記述するテキストです。
# "#" 以降の文字はコメント扱いになります。

# " 項目名 = 値 " の形式で記述してください。クォートなどは不要です。
# Python上ではすべて config_module[<項目名>] で取り出せる文字列型として現れます。
# 都合上、"#" や "="、改行 を含む文字列は扱うことができません。
# また、項目名や値の前後につくスペースなども消去されます。

# OpenAI APIキーです。自分で取得してください。
api_key = otameshi-no-api-key
